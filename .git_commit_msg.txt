fix(docker): use --no-frozen-lockfile + fix pnpm-workspace allowBuilds

pnpm 11 requires explicit build script approval per project.
The pnpm-workspace.yaml allowBuilds config was created with placeholder
values. Fix: set all required packages to true.

Use --no-frozen-lockfile in Dockerfiles so pnpm reads allowBuilds
from pnpm-workspace.yaml at install time without interactive approval.

Changes:
- backend/pnpm-workspace.yaml: set allowBuilds true for all 4 packages
- backend/Dockerfile: pnpm install --no-frozen-lockfile
- frontend/Dockerfile: pnpm install --no-frozen-lockfile
