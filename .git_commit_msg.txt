fix(frontend): use useContext(AuthContext) instead of non-existent useAuth

PrivacyCompliancePage.tsx imported useAuth which is not exported
by AuthContext.tsx. The project uses the raw useContext(AuthContext)
pattern. Fix: import useContext + AuthContext, use ctx?.user instead.
