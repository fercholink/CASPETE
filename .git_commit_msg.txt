feat(compliance): add back button and CSV export to privacy panel

- Button "← Panel principal": navigates back to /dashboard
- Button "⬇ Descargar informe CSV": exports current tab data as CSV
  - Audit Log tab: exports all visible log entries with ID, fecha,
    accion, entidad, usuario, email, rol, IP, campos, justificacion
  - ARCO tab: exports all ARCO requests with status and resolution date
  - CSV uses semicolon separator + UTF-8 BOM so Excel opens correctly
  - Filename includes date: caspete_audit_log_14-05-2026.csv
